<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="res_config_settings_view_form_i" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.bahmni.sale</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="1"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Bahmni Sale" string="Bahmni Sale" name="bahmni_sale">
                    <block title="Sale Configurations" id="bahmni_sale_configurations">
                        <setting id="is_delivery_automated" help="On sale order confirm action delivery order will be auto confirmed.">
                            <field name="is_delivery_automated"/>
                        </setting>
                        <setting id="sale_price_markup" title="Sale Price Markup Rule" help="Determine sale price based on cost price markup">
                            <field name="sale_price_markup"/>
                        </setting>
                        <setting id="is_invoice_automated" help="On sale order confirm action invoice will be auto confirmed.">
                            <field name="is_invoice_automated"/>
                        </setting>
                        <setting id="allocate_quantity_from_multiple_batches" title="Allocate quantity from multiple batches" help="Quotation should select product from multiple batches if the nearest expiry batch does not have sufficient quantity when order syncs from Bahmni.">
                            <field name="allocate_quantity_from_multiple_batches"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="res_config_settings_action" model="ir.actions.act_window">
        <field name="name">Configuration</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'module' : 'bahmni_sale'}</field>
    </record>
</odoo>